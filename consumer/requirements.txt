# =============================================================================
# Archer Consumer -- Dependencies
# =============================================================================
# kafka-python-ng : Maintained fork of kafka-python (original is broken on 3.12+)
# transformers    : HuggingFace library for loading FinBERT sentiment model
# torch           : PyTorch backend required by transformers for inference
#                   NOTE: In Docker, torch is installed separately with CPU-only
#                   wheels (see Dockerfile). For local dev: pip install torch==2.6.0
# scipy           : Required by some transformers model outputs (softmax, etc.)
# =============================================================================
kafka-python-ng==2.2.3
transformers==5.9.0
scipy==1.14.1
sqlalchemy==2.0.50
psycopg2-binary==2.9.12
requests==2.32.3

